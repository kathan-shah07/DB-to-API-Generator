# 🚀 DB-to-API Generator: Quick Start

Welcome! This bundle contains everything you need to generate high-performance APIs from your SQL database.

## 📁 Package Structure
*   **Setup_DB_to_API.exe**: 🛠 Run this first! It installs all system pre-requisites (Python, Node, ODBC Drivers) and initializes the local environment.
*   **Run_DB_to_API_Server.exe**: ⚡ Run this once setup is complete to start the server and open the dashboard.
*   **backend/**: The core engine source code.
*   **Documentation/**: Contains detailed guides, architecture maps, and demo instructions.
*   **frontend/**: Contains pre-built UI assets.

## 🛠 One-Time Setup
1.  Double-click **`Setup_DB_to_API.exe`**.
2.  Grant Administrator permissions when prompted (required for system-wide runtimes).
3.  Wait for the console to finish (it will install Python 3.11, Node.js, and SQL drivers).

## 🚀 Running the App
1.  Double-click **`Run_DB_to_API_Server.exe`**.
2.  A terminal window will show your **Admin Key**.
3.  Your browser will open to `http://localhost:8000`.
4.  Paste the **Admin Key** into the top-right field and click **Save**.

## 📖 Need Help?
Check the `Documentation/` folder for:
*   `INSTALLATION.md`: For manual setup instructions.
*   `DEMO_GUIDE.md`: For a step-by-step walkthrough of building your first API.
*   `ARCHITECTURE.md`: To understand how the engine works.

---
*Created by Antigravity*
